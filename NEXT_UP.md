- Let's implement persitence with SQLite for simplicity (repository layer should be simple to switch, keep in-memory as a ENV var setting, include .env functionality)
- Let's install the used claude skills in .claude locally
- Let's deploy this k8s (if not working already)